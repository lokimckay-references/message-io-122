{
  "name": "message-io-122",
  "version": "0.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "run-p dev:browser dev:rust",
    "dev:browser": "serve ./browser",
    "dev:rust": "pushd rust && cargo run && popd"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "serve": "^13.0.2"
  }
}
